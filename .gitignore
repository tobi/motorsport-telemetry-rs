/target/
/build/
/configure/
*.duckdb
.DS_Store

/wasm-extension/target/
/web/node_modules/
/web/dist/
/web/public/

# Telemetry data is private and often large. The checked-in fixtures are small,
# deterministic synthetic files; real telemetry logs must never be committed.
*.pds
*.ld
*.ldx
*.vbo
*.parquet
